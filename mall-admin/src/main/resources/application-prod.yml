spring:
  datasource:
    url: jdbc:mysql://db:3306/your_database?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: your_db_username
    password: your_db_password
    druid:
      initial-size: 5 #连接池初始化大小
      min-idle: 10 #最小空闲连接数
      max-active: 20 #最大连接数
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
      stat-view-servlet: #访问监控网页的登录用户名和密码
        login-username: your_druid_username
        login-password: your_druid_password
  redis:
    host: redis # Redis服务器地址
    database: 0 # Redis数据库索引（默认为0）
    port: 6379 # Redis服务器连接端口
    password: your_redis_password # Redis服务器连接密码
    timeout: 300ms # 连接超时时间（毫秒）

minio:
  endpoint: http://your-minio-host:9090 #MinIO服务所在地址
  bucketName: your_bucket_name #存储桶名称
  accessKey: your_minio_access_key #访问的key
  secretKey: your_minio_secret_key #访问的秘钥

logging:
  file:
    path: /var/logs
  level:
    root: info
    com.macro.mall: info

logstash:
  host: logstash